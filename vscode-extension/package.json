{
  "name": "debate-ext",
  "displayName": "DEBATE EXT",
  "description": "AI debate orchestrator — Claude + OpenAI collaborate via API keys to produce robust code",
  "version": "1.0.0",
  "publisher": "debate-ext",
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Programming Languages"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "debate-ext",
          "title": "DEBATE EXT",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "debate-ext": [
        {
          "type": "webview",
          "id": "debateExt.chatView",
          "name": "Chat"
        }
      ]
    },
    "commands": [
      {
        "command": "debateExt.runDebate",
        "title": "DEBATE EXT: Run Debate (auto)",
        "icon": "$(play)"
      },
      {
        "command": "debateExt.runSimple",
        "title": "DEBATE EXT: Run Simple"
      },
      {
        "command": "debateExt.runComplex",
        "title": "DEBATE EXT: Run Complex"
      },
      {
        "command": "debateExt.showLogs",
        "title": "DEBATE EXT: Show Logs",
        "icon": "$(output)"
      },
      {
        "command": "debateExt.configureCLIs",
        "title": "DEBATE EXT: Configuration",
        "icon": "$(gear)"
      },
      {
        "command": "debateExt.configureOpenAIKey",
        "title": "DEBATE EXT: Configure OpenAI Key",
        "icon": "$(key)"
      },
      {
        "command": "debateExt.configureAnthropicKey",
        "title": "DEBATE EXT: Configure Anthropic Key",
        "icon": "$(key)"
      },
      {
        "command": "debateExt.clearChat",
        "title": "DEBATE EXT: Clear Chat",
        "icon": "$(clear-all)"
      },
      {
        "command": "debateExt.stopDebate",
        "title": "DEBATE EXT: Stop Debate",
        "icon": "$(debug-stop)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "debateExt.runDebate",
          "when": "view == debateExt.chatView",
          "group": "navigation@1"
        },
        {
          "command": "debateExt.stopDebate",
          "when": "view == debateExt.chatView",
          "group": "navigation@2"
        },
        {
          "command": "debateExt.clearChat",
          "when": "view == debateExt.chatView",
          "group": "navigation@3"
        },
        {
          "command": "debateExt.configureCLIs",
          "when": "view == debateExt.chatView",
          "group": "navigation@4"
        }
      ]
    },
    "configuration": {
      "title": "DEBATE EXT",
      "properties": {
        "debateExt.claudeModel": {
          "type": "string",
          "default": "claude-sonnet-4-20250514",
          "enum": ["claude-sonnet-4-20250514", "claude-opus-4-20250514", "claude-haiku-3-5-20241022"],
          "description": "Claude model to use (Anthropic API)"
        },
        "debateExt.claudeTimeout": {
          "type": "number",
          "default": 300000,
          "description": "Timeout for Claude API calls in milliseconds"
        },
        "debateExt.openaiModel": {
          "type": "string",
          "default": "gpt-4o",
          "enum": ["gpt-4o", "gpt-4o-mini", "gpt-4-turbo", "o1", "o1-mini", "o3-mini"],
          "description": "OpenAI model to use for the consultant"
        },
        "debateExt.openaiTimeout": {
          "type": "number",
          "default": 300000,
          "description": "Timeout for OpenAI API calls in milliseconds"
        },
        "debateExt.testCommand": {
          "type": "string",
          "default": "",
          "description": "Optional test command to run between iterations (e.g., pytest -q, npm test)"
        },
        "debateExt.pythonPath": {
          "type": "string",
          "default": "",
          "description": "Path to the Python venv for auxiliary scripts (auto-detected if empty)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "benchmark": "npm run compile && node out/benchmark.js",
    "bench2": "npm run compile && node out/benchmark_v2.js",
    "bench:mbppplus": "npm run compile && node out/benchmark_paper.js",
    "smoke": "npm run compile && node scripts/smoke.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
}
